(32) Build a RAG Chatbot from Scratch | React, Next.js, AI SDK, AI Elements, Neon, Drizzle, Clerk - YouTube
https://www.youtube.com/watch?v=3E5OxozYuA8

Transcript:
(00:00) AI is amazing, but it knows nothing about your data. Chat GPD can write poetry, explain quantum physics, and code entire applications. But ask about your company policies, your research papers, or your personal notes, and it has no clue. Today, we're solving this problem by building a rack chatbot that turns any PDF into instant AI knowledge.
(00:25) But before we start coding, let's first understand what Rag actually is and why it's important. Imagine you're building a chatbot for your company. Maybe an internal tool where employees can ask questions about company policies, benefits, or procedures. Now, the problem is that AI models have never seen your company's data.
(00:45) They don't know your vacation policy, your expense reimbursement process, or any of your internal information. So, with a traditional chatbot, when someone asks, "What's our vacation policy?" or "How many sick days do I get?" the AI can't answer because it simply doesn't have that information. That's where rack comes in.
(01:08) Rack stands for retrieval augmented generation. Retrieval means we search for and retrieve relevant information from your own data sources. Augmented means we enhance the AI's capabilities by providing this retrieved information. Generation means the AI generates a response based on both its training and the information we provided.
(01:32) Instead of the AI trying to remember everything, we give it access to your company's knowledge base. When someone asks a question, we quickly find the relevant documents, hand them to the AI, and then the AI uses those documents to generate an accurate answer. The really cool part is that this works instantly with any updates. If you changed the vacation policy last week or if you added a new benefits package, it is immediately available.
(01:59) The AI becomes a knowledge assistant that has access to all your current documentation. So that is what we're building today, a rag chatbot. By the end of this tutorial, you'll have built a rag application with realtime streaming chat interface, PDF upload and processing, vector embeddings for semantic search, beautiful UI with Shatsian and AI elements, user authentication and role-based access control with Clerk, and production ready database with Neon.
(02:30) Whether you're building an internal tool for your team, a customer support chatbot, or just learning to build AI applications, this tutorial will give you a solid foundation in modern rag architecture. And to understand the project to its fullest, please make sure to watch the next AI SDK playlist on the channel. Let's start building.
(02:52) First, create a new NexJS project. In the terminal, run the command npx create next app at latest. And we want to create the project in the current directory. When prompted, select yes for typescript, biome as your llinter, tailwind CSS, yes to source directory, yes to app router, turbo pack, and no to import alias.
(03:19) When the command completes, you will have a new NexJS app ready to go. Run the app with npm rundev and in the browser open localhost 3000. You should see the Nexus welcome page. Now that we have our Nexus app set up, let's build a standard AI chatbot. The user will enter a prompt. We will send it to a route handler that interfaces with OpenAI and stream the response back to the UI.
(03:48) Back in VS Code, let's install the AI SDK packages we need. npm install AI at AI SDK/OPAI at AI SDK/REAC and ZOD. AI is the core AI SDK package. AI SDK/OPAI connects us to OpenAI's models. AI SDK/Ract contains React hooks for building chat interfaces and ZOD helps us create Typesafe schemas. Next, let's set up the UI components. We'll use shaden UI and AI elements.
(04:22) In the terminal, run the command npx shaden at latest space in it. Select neutral and then run the command npx shaden at latest space add. We'll keep this simple and select all the components. Press A to select all and press enter. Next, we'll add AI elements. The command is npx ai-en elements at latest.
(04:50) This gives us pre-built components specifically for AI applications. If we expand the source folder, components folder, we can see shaden components in the UI folder and our AI specific components in the AI elements folder. Now before we write any code, we need to add our openi API key. In the project root, create av.
(05:13) local local file. Add a new variable called open AAI API_E and add your actual OpenAI API key. I'm going to do this behind the scenes. Now, let's create the chat route handler. In the app folder, create a new folder called API. Within this folder, create a new folder called chat with a route.ts file. Start with a basic post handler.
(05:41) export async function post receives request of type request and empty curly braces. Next, let's add our imports at the top of the file. Import the stream text function from the core AI package and the OpenAI provider from AI SDK/OPAI package. These work together to stream text using OpenAI's models.
(06:09) We will also import the UI message type and convert to model messages helper function from the AI package. Within the post function, let's get the messages from the request. Await request.json. This returns an object from which we destructure messages. The type of messages is UI message array.
(06:38) Next, let's call stream text to generate the response. const result is equal to stream text. We pass in an object with model set to open AAI GPT 4.1 mini and messages set to convert to model messages passing in messages. We use the helper function to convert the messages from the UI to the format expected by the model. Now I'm using GPD4.1 mini for this tutorial.
(07:09) But if you want to use GPD 5 mini, there is an extra step you need to take if your organization has zero data retention policy. But I'll keep it simple and use GPT4.1 mini. So I recommend you do the same to follow along. Once we have the result, let's return the streaming response. So return result dot to UUI message stream response.
(07:34) Let's also add error handling with a try catch block. So try closing curly braces catch. You get hold of the error console error error streaming chat completion and log the error and we return a new response fail to stream chat completion status 500. Perfect. Our route handler is complete. Now let's build the UI. In the app folder, create a new folder called chat with a page.tsx file.
(08:11) Here, define a basic react component. Export default function rag chatbot and we return a div tag. This component will be a client component since we'll need interactivity. So add the use client directive at the top of the file. Now, the AI elements package gives us beautiful components for chat interfaces.
(08:35) Let's import a couple of them. To save us some time, I'm going to paste the imports here. You can copy these from the GitHub repo linked in the description, but also feel free to style the UI to your liking. You can take a look at the AI elements docs for detailed information on each component. But the first set here is related to prompt input and submission.
(08:57) The second set here related to rendering the conversation and loader is basically a loading indicator. Now that we have the components imported, let's start building the JSX structure. First, we'll create a container that centers everything and takes the full height. So div class name is equal to max width for Excel MX auto padding 6 relative size full and the height for now is going to be the 100% of viewport height.
(09:31) So calculate 100 VH. Within this div tag, we're going to nest another div tag. This one with class name is equal to flex flex column to vertically place our items and full height. Now let's add the conversation component. So conversation last name is equal to full height. And within the conversation component, we're going to nest conversation content.
(10:02) And this is where our messages will go. And then a conversation scroll button component. The conversation component handles the scrollable area. And the conversation scroll button adds a nice button to scroll to the bottom. Below the conversation, we will add the prompt input. So prompt input. We'll add some top margin and we will nest a couple of components. First, prompt input body.
(10:30) This will contain the prompt input text area and below the text area, prompt input toolbar with the submit button. Optionally, you can also include prompt input tools for elements like a model selector, the web search icon, extended thinking, and so on. We will not implement this for our chatbot, but I will leave a placeholder here for you to understand where the additional functionality can go.
(11:00) There is a type error on prompt input, but we will fix this in a bit. But we now have the structure in place. Of course, nothing works because we haven't connected it to any logic. So, let's add state for the input field. Import use state from React. And we will also import the fragment component which we will need for rendering messages in a bit.
(11:27) Within the component, let's add the state. So use state. The variable is called input. The setter function set input and the initial value is an empty string. We will bind this to prompt input text area. So value is equal to input and on change is equal to an arrow function which receives the event and we call set input event.target dot value.
(11:57) Next for managing the chat state we will use the use chat hook from AI SDK/react package. Import it at the top. Invoke the hook within the component. use chat. This hook gives us an object from which we can dstructure messages which is the conversation history send message which is a function to send new messages to the API route and status to know status of the chat.
(12:29) By default this hook automatically connects to / API/ chatad which is exactly what we have created. So please make sure to follow the same route structure. Now let's render the messages inside conversation content curly braces messages do map pass in a callback function for each message render a div tag with key is equal to message do ID and each message has parts allowing the messages array to contain different types of content.
(13:05) Let's map through the parts. Curly braces message dotparts dot map. We pass in a callback function. We get access to the individual part as well as the index. And within the function body, we will use a switch statement to handle the text part type which is the only part type we'll have for this implementation.
(13:29) So switch statement on part.ype type curly braces and for case text we return react fragment with key set to dollar curly braces message do ID followed by the index to make sure this is unique. We will then invoke the message component with from set to message roll. And this prop tells the message component whether this is from the user or assistant.
(14:03) The text will get styled appropriately. Within the message, we render message content with the response component rendering part.ext. The response component handles the markdown rendering. We'll also add a default case where we return null. Next, let's use the status enum and add a loading indicator that shows while we're waiting for a response.
(14:30) So, right before ending conversation content, curly braces if status is equal to submitted or status is equal to streaming render the loader. Finally, we'll add a handler that calls send message for when the user submits a message. const handle submit is a function receives message of type prompt input message and within the function body if there is no text.
(15:05) So if there is no message text we don't send anything we simply return. Otherwise, we send the message passing in an object with text set to message.ext and clear the input. So, set input empty string. This message, by the way, is automatically passed as an argument to handle submit when we assign it to the onsubmit prompt of the prompt input component.
(15:34) So, on prompt input, onsubmit is equal to handle submit. Our TypeScript error is also fixed. Perfect. Our UI is now complete. Time to test our standard AI chatbot. Navigate to localhost 3000/Chat and you should see the clean chat interface. Enter the prompt, what is React and submit. The AI will stream back a response.
(16:02) Try a follow-up like can you give me a code example and the chat maintains context and provides a relevant example. But now watch what happens when you ask what is our company's vacation policy. The AI can't help because it doesn't have access to that information. But before you give AI access to internal information, you would want to control who has access.
(16:32) For this next section, let's add authentication with Clerk, our sponsor for this video. Clerk gives us a complete authentication solution with pre-built UI components and user management. Click on the link in the description to follow along. First, sign up to create an account if you haven't already.
(16:50) I'm going to enter my email and password. Verify your email. And now, once you're logged in, create a new application. Enter the name of the application as Rag Chatbot. Select sign-in options that you want to support. And you can see a preview of the UI on the right. With the default options, we will have email, password, and Google authentication.
(17:13) I will also add GitHub. You can see the same updated in the preview. When you're happy, click on the create application button. This brings us to the instructions page. Choose Nex.js as a framework if it's not already selected. Now, let's follow the steps together to add Clerk to our project.
(17:35) Step one, install the Clerk package. Copy the command from the setup instructions page and run it in your terminal. Step two, set up your Clerk API keys. Copy the publishable key and secret key from the setup instructions page and add them to your env.local file. I just completed that step behind the scenes, but make sure to use your own keys instead of mine.
(18:04) Step three, create a middleware.ts file in the source folder. So within the source folder, new file, middleware.ts, copy the code from the setup instructions page and paste it in the file. This will help us access O data and configure protected routes later. Step four, wrap your entire application with the clerk provider component, which is a React component that provides clerk's context to your application.
(18:34) We will do this a little differently to what the setup instructions show. In the root layout file, so layout.tsx, import clerk provider component at the top from at clerk/nex.js JS and wrap the JSX with it. So return clerk provider. All right. Now that we have clerk set up, let's add signin and sign out functionality to our application.
(19:04) Inside the components folder, create a new file called navigation.tsx. We will define and export a react component called navigation. So export const navigation is equal to an arrow function. Now the great thing about clerk is that it provides pre-built components that handle all the authentication flows for us.
(19:27) We will import the sign-in button, the sign out button, the sign up button, the signed in component and the signed out component from clerk. We will also import the button component from Shatsian UI. For the JSX, we will start with the nav element with a border and a flex container nested inside it. Within the container, we'll add a logo of sorts. So, a div tag.
(19:59) Let's call this rag chatbot with class name text excel font semi bold. We will also add a div for the signin and sign out buttons. a flex class to place the buttons side by side and gap two for some spacing. Now within the div, let's use the cler components. The signed out component renders the children when the user is not signed in.
(20:26) In that case, we will render the sign-in button and sign up button components using the shadsen button component. So sign in button with shhatsen button the text is sign in variant is equal to ghost and we will also add mode is equal to model to open a model dialogue instead of redirecting to a separate page.
(20:54) Right next to the sign-in button, we will add the sign up button. Mode is equal to modal. We will use shaden button component once again and the text is sign up. Along similar lines, we invoke the signed in component which renders the children when the user is signed in. So in that case, we will render the sign out button component using the shadian button element.
(21:20) Sign out button. the shadian button component with the text sign out and variant is equal to outline. Our navigation is ready. Include this in your layout.tsx file within the body tag. Import and invoke the navigation component. Import it at the top from components/navigation. To accommodate for the navigation in page.tsx, tsx.
(21:51) We're going to remove some height from the page. Check the browser and we see the navbar with the logo top left and the sign in and sign out buttons top right. Let's test if the o flow works as expected. Click on the sign up button and create an account with your email and password. Verify your email and once signed up, you should be redirected back to the homepage.
(22:18) We now see the sign out button instead of sign in or sign up. Click on the sign out button and you're logged out. Redirected to the homepage. Click sign in. Enter your email and password and you should be redirected back to the homepage. Perfect. Our o setup works as expected. For the next step, let's protect our chat route.
(22:48) So only authenticated users can access it. Right now anyone can go to / chat which is not great for internal information. So let's add this protection logic in the middleware.ts file. First we'll import a function called create route matcher from clerk which helps us define which routes are public. So const is public route is equal to create route matcher.
(23:14) We pass in an array the root route along with sign in and sign up. We're saying that the homepage and O pages are public. Everything else needs authentication. Next, we'll pass a function to clerk middleware. This is an async function which receives the O object and the request object as arguments. In the function we check if the request does not match any of the public routes.
(23:45) So if not is public route passing in request and if that is the case we call the protect method on the o object. So await o.protect. This method will redirect the user to the sign-in page automatically if they're not signed in. You can see how easy it is to implement route protection. Let's test if it works as expected.
(24:13) Sign out and navigate to localhost 3000/hat. You should be redirected to the sign-in page. Sign in with your email and password. And once signed in, you should be automatically redirected back to the chat page. Our soon tobe internal rack chatbot is now protected and only authenticated users can access it. For the next step, let's add the ability to upload knowledge documents that our chatbot can use to answer questions.
(24:42) This feature involves three separate parts that all work together. First, we need a UI where users can upload documents. We will keep it simple and stick to PDF documents for this project. Second, we need a server action that processes these PDFs. The server action will extract the text from the uploaded PDF, chunk it into smaller pieces, and then convert those chunks into vector embeddings using OpenAI's embedding model.
(25:15) Chunking helps improve the accuracy and efficiency of semantic search and is a crucial step in RAG. Third, we need to store the vector embeddings in a vector database. This becomes our knowledge base that we will search against when users ask questions in the chat. Let's start with the database setup first as we need a place to store the embeddings before we can process any PDFs.
(25:40) For our vector database, we will use Neon. Neon is a serverless Postgress database that supports vector operations. Perfect for storing our embeddings. Head over to neon.com and create an account. Once you're logged in, let's create a new project. Name it rag chatbot. For the Postgress version, select 17.
(26:06) Choose AWS as the provider and pick a region close to you. Then click create project. Once the project is created, you will see a connect to your database card. Click connect and click the copy snippet button to copy the connection string. Now let's add this to our enenv.local file neon database URL and paste the string without the psql prefix.
(26:35) All right, that's all we need from the neon console. Everything else we'll do with code. So let's install the packages we need. In the terminal, run the command npm install at neon database/serverless. Drizzle-kit and env. Neon database/serverless is Neon's database driver for TypeScript.
(27:02) Drizzle OM is a type safe OM that lets us write TypeScript instead of SQL to work with our database. Drizzle Kit is the CLI tool we'll use for handling database migrations or changes. and env helps us load environment variables. All right, now let's set up our database connection. Inside the lib folder, create a new file db-config.ts. First, we'll import the functions we need.
(27:32) import drizzle from drizzlem/neonhttp which creates our database instance and also import neon from neon database/serverless to create the connection. We'll also import config from enenv to load our environment variables. Now let's make use of the imports. First call config passing in an object with path set to env.local. We're basically loading the environment variables from our env.local file.
(28:05) Next, create a SQL client with our connection string. So, const SQL is equal to neon process envon database URL. And we use the exclamation to let TypeScript know we know what we're doing. Finally, wrap SQL with Drizzle. So export const DB is equal to Drizzle SQL. A database connection is now ready.
(28:35) Next, let's define our database schema. In the same lib folder, create a new file called DB schema.ts. First, import the types we need from Drizzle. PG table serial text and vector. Now let's define our documents table. export const documents is equal to PG table. We'll call it documents. We'll have ID of type serial ID and this is primary key.
(29:09) We'll have our content which is text and this cannot be null. Finally, we'll have our embedding. This is of type vector and we specify dimensions 1 536. So the ID is an auto incrementing primary key. The content field stores the actual text chunk from your PDF document. And embedding is a vector field with,536 dimensions which is the size of OpenAI's text embedding 3 small model output.
(29:42) But searching through vectors can be slow if you don't have an index. So let's add one import index and as a third argument to PG table specify a function which returns an array. The function gets access to table and we call index. Let's name this embedding index dot using hnssw table dot embedding op vector cosine ops.
(30:16) If this is confusing, don't worry too much about it. It's just syntax you have to follow. This basically creates an HNSW index, which stands for hierarchical navigable small world. And it's an algorithm that makes similarity searches really fast. Now, let's also export types for TypeScript. So, export type insert document is equal to type of documents dot dollar infer insert and similarly export type select document type of documents dotd dollar infer select our schema is ready the next step is to configure drizzle in the root of your project create a drizzleconfigts
(31:05) file here import config from env and define config config from Drizzle kit. Next, load the environment variables. So, config path env.lo and then export default define config pass in an object. We set schema to do slash source lib db schema.ts TS out to do /migrations dialect Postgress and DB credentials URL process envon database URL with exclamation.
(31:56) So this tells Drizzle where our schema is, where to put migrations and how to connect to the database. Now here is an important step. Neon's postress needs the pg vector extension to work with vectors. So first let's create a custom migration to enable it. In the terminal run the command npx drikit generate d-custom.
(32:22) This creates a new migration file in the migrations folder. Open that file and add create extension if not exists vector. Now run the migration to enable the extension npx drizzle-k space migrate. Our command completes. And now let's verify if it worked. Go to your neon console. Open the SQL editor and run. Select extension name, extension version from pg extension where extension name is equal to vector.
(33:02) You should see the vector extension listed with the version number. Now we can generate the migration for our documents table. Run the command npx drizzle kit generate. This will create a new migration file in the migrations folder. We can see the SQL create table documents ID content embedding and an index. Now you can run npx drikit migrate.
(33:30) Our command completes and to verify everything worked back in neon console. Click on tables and you should see the documents table. We have ID content and embedding which is vector of,536 dimensions. Perfect. Our vector database is now ready. We have a place to store our document chunks and their corresponding embeddings and we have set the HNSW index to support fast similarity search.
(34:02) The next step is to create our server action for uploading documents. But before we build that server action, we need a couple of utility functions it will use. First, we need utility functions for generating embeddings. When we process a PDF, we'll need to generate embeddings for multiple chunks of text.
(34:21) And later, when a user searches, we'll need to generate an embedding for their search query. Let's create both. Back in VS Code, create a new file in the lib folder called embeddings.ts. Begin by importing the embed function from the AI SDK and the OpenAI provider. Now let's start with the single embedding function which we will use for search queries.
(34:48) Export async function generate embedding. This takes a string and returns an array of numbers which is the embedding vector. Inside the function we'll clean up the text. So const input is equal to text dotreplace new line character with an empty space. And then we'll generate the embedding. Await embed.
(35:14) We pass in an object model is set to openai.ext embedding model and we use text embedding 3 small which produces,536 dimensional vectors exactly what we configured in our database schema. We will also pass input as the value. This returns an object from which we can dstructure the embedding. we return that embedding.
(35:41) Now for processing PDFs, we'll have multiple chunks to embed. We could call generate embedding for each chunk, but that means multiple API calls, which is slow and expensive. Instead, let's create a batch function. Import embed many from AI SDK and create the batch function. Export async function. generate embeddings plural. This gets texts which is an array of string and within the function body const is equal to text dot map and for each text we replace new line with a blank space and then we call embed menu with the same model. So, OpenAI text
(36:29) embedding model and values plural is going to be inputs plural. This returns an array of embeddings which we can dstructure. Finally, return embeddings from the function. Perfect. We now have utilities for generating embeddings. Singular for search queries and patch for document processing. Next, let's create the chunking utility.
(36:59) Remember, we need to break PDFs into smaller pieces so we can retrieve just the relevant sections when users ask questions. We'll use a package called lang chain that has utilities specifically for texts spplitting. Back in VS Code, run the command npm install at langchain/extsplitters. Now create a new file in the lip folder called chunking.ts.
(37:27) chunking.ts. First import a function called recursive character text spplitter from add langchain/extplitters. Then invoke the function export const text splitter is equal to new recursive character text splitter. We pass in an object chunk size set to 150 and chunk overlap set to 20. Chunk size 150 means each chunk of text will be roughly 150 characters.
(38:09) That is pretty small but for this demo it works well. In production you might use 500 or thousand characters. Chunk overlap 20 means consecutive chunks will share 20 characters at their boundaries. This helps us maintain context across chunks and is very important. And let's add one more parameter to make the splitting cleaner.
(38:34) Separators is an array and we specify just one which is a single space. This prevents words from being cut in half. Now let's create the function that uses this splitter. So export async function chunk content receives content of type string which will be the PDF content in our case. And we call the split text method text splitter dotsplit text and pass in the content.
(39:04) We'll trim the content to remove any leading or trailing white space. So content dot trim. The function will return an array of strings and each string is a chunk. So return await textsplitter.split text. That's it. Our chunking utility is now ready. It takes a long piece of text and breaks it into smaller overlapping chunks that are perfect for embedding and searching.
(39:31) We have both the utilities our server action needs. Emitting generation and text chunking. Now let's build the server action that brings everything together. We'll extract text from PDFs, chunk it, and generate embeddings and store everything in our database. First we need a package to extract text from PDFs.
(39:57) So in the terminal run the command npm install PDF- parse. We need to treat PDF parse as external in the server build. So update next.config.ts to include it. The configuration is server external packages and we specify PDF parse. Next, create a new folder in the app directory called upload and inside create a file actions.ts. Add the use server directive at the top of the file.
(40:32) This tells NexJS this is a server action which is code that runs on the server but can be called directly from client components. Now let's import what we need. We import the PDF parser. So PDF from PDF parse our database connection. So import DB from lib/dbconfig the documents table schema and the two utilities we just created generate embeddings plural and chunk content.
(41:06) Now we have an error for PDF parse because we haven't installed the types. So run the command npm install at types/pdfen parse. Perfect. Now let's create the function that processes PDFs. Export async function process PDF file. And this function receives form data from the upload form. Form data of type form data.
(41:37) Now form data is how browsers send files to servers. within the function body. Let's extract the PDF file. We'll start with a try catch block for catching any errors. So, console dot error PDF processing error logging the error. And we return an object with success set to false and error set to failed to process PDF. Within the try block we get the file from form data.
(42:11) So const file is equal to form data.get PDF as file. Now to extract text from the PDF we need to convert the file to a buffer and then pass it to PDF parse. So const bytes is equal to await file dot array buffer and then const buffer is equal to buffer dot from bytes and finally const data is equal to await pdf buffer.
(42:46) So first we convert the file to an array buffer then to a nodejs buffer then pass it to pdf parse which extracts the text. Next, let's check if we actually got any text. So, if there's no data.ext or data.ext dot trim dotlength is equal to zero, we return success false and error no text found in PDF. So, if the PDF is empty or has only images, we return an error.
(43:23) But if it does contain text, let's use our chunking utility. So const chunks is equal to await chunk content and we pass in data.ext. This breaks the entire PDF text into smaller chunks. Next, generate embeddings for all the chunks. So const embeddings is equal to await generate embeddings and we pass in chunks. We use our batch function to generate all embeddings in one API call.
(43:55) Now let's prepare the data for our database. So const records is equal to chunks dot map. We pass in a callback function. We get access to individual chunk as well as the index and we return an object where content is set to chunk and embedding is set to embeddings of index. So we create an array of records where each record has the text chunk and its corresponding embedding.
(44:30) The chunks and embeddings are in the same order. So we use the index to match them up. Finally, insert all records into the database. Await db.insert documents and the values we insert are the records. We then return an object where success is set to true and message is set to created records. Searchable chunks.
(45:01) All right, our server action is complete. It handles the entire flow from receiving a PDF file to storing searchable chunks in our vector database. Now let's build the UI where users can upload PDFs. This will call the server action we just created. In the upload folder we created earlier. Add a page.tsx file. Let's define a basic React component.
(45:27) Export default function PDF upload. and we return a dev tag. This is a client component since we'll need to handle file uploads and show loading states. So add the use client directive at the top. Next, let's import what we need. We need use state for managing component state and our server action. So import process PDF file from dot / actions.
(45:56) Now let's import the UI components. We need card and card content, label, input, alerts, and loader. These are shaden UI components we'll use for the UI. Next, let's set up the state we need inside the component invoke use state. The variable is loading. The setter function set is loading and the initial value is false.
(46:25) We will also track any messages to show the user. So you state messages set messages and the initial value is null. The type however is an object where we have type property that is either error or success and text property a string. It is either this object or null. Now let's build the JSX structure. Start with the container. So a div tag with class min height screen background gray 50 some vertical padding and horizontal padding.
(47:06) Next another div tag with class name max width 4xl and mx auto. Next add the page title. So h1 pdf upload. We'll also add some Tailwind classes for styling text 3 XL font bold text gray 900 margin bottom 8 text center. Now add the upload card. So invoke the card component from shad cnen with class name margin bottom six. invoke the card content component inside with class name padding top six and we'll add a div with class space y4 this will add some spacing between the label and the input which we will define now so invoke the label component label
(48:01) is upload pdf file we will add html 4 is equal to pdf upload and below the label the input component ID is equal to PDF- upload. Type is equal to file. We set accept is equal to PDF to only allow PDF files. On change is equal to handle file load which we will define in a moment. Disabled is equal to ease loading and some more top margin.
(48:37) Below the input add the loading indicator. So curly braces is loading and render a div tag with class name flex item center gap 2 loader two with class name is equal to height five width five animate spin and span tag that says processing PDF. We will also add some styles. Text muted foreground. This shows a spinning icon and message while processing.
(49:14) Loading should actually be outside the div tag. Finally, add the message display. So braces message and if that exists, we render the alert component. We nest the alert title and the alert description. We have cannot find message. The state variable should be message and not messages. So message and set message. Back in the JSX for alert, we're going to specify variant is equal to if message dot type is equal to error destructive otherwise default.
(49:54) For the alert title, if the message type is error, we render the text error. Otherwise, we render the text success. And for the alert description, we render message.ext. So, the alert shows success or error messages. The variant changes the styling based on the message type. Now, let's create the file upload handler that the input calls on change.
(50:22) const handle file upload is equal to an async function. It receives event of type react dot change event HTML input element and within the function body we get the selected file. So const file is equal to e.target target dot files and if that exists the first file and if there is no file we simply return set east is loading to true and set message to null to clear any previous messages.
(50:58) Now let's call our server action. We'll add a try block with catch and if there is an error we set message passing in an object where we set type to error and text an error occurred while processing the PDF. We will also add a finally block where we set is loading to false in the try block.
(51:29) However, we create form data. So const form data is equal to new form data and we append the file. So form data dot append the file with the key PDF. This is what our server action expects. So call the server action process PDF file passing in the form data and handle the result constuccessful. So result dot success make sure to await the server action.
(52:05) So if result dot success call set message passing in type success and text set to result dot message and if that doesn't exist PDF processed successfully we will also set e.target dot value is equal to an empty string. So we are clearing the file input. If the result is not successful from the server action we call set message we set type to error and text result dot error or failed to process PDF.
(52:47) And that is our PDF upload UI. Let's test this in the browser. Navigate to localhost 3000/upload. You should see the upload interface. Try uploading a PDF file. I'm going to upload this Tech Corp Solutions employee handbook which is a PDF file I had generated from AI. It has a few sections like health and wellness benefits, professional development, code of conduct, performance reviews, expense reimbursement policy and communication guidelines.
(53:22) You can upload any PDF you want to. So upload and you will see a loading spinner. Then a success message telling you how many chunks were created. Success created 28 searchable chunks. If you take a look at Neon and refresh the documents table, you will see the chunks are now stored in your vector database with their embeddings ready to be searched.
(53:49) We have content and the corresponding embedding. And for each chunk, if I were to open the third one for example, you can see we start with balance while delivering and end with through innovative technology. If I go to the previous one, you can see the overlap balance while and the one after technology. Our PDF upload works as expected.
(54:14) We can now process PDFs, extract their text, chunk them, generate embeddings, and store everything in our vector database. Next, we need to implement semantic search so our chatbot can find relevant chunks when users ask questions. Back in VS Code, we'll create a search function that finds similar documents based on meaning.
(54:38) So, create a new file in the lib folder called search.ts. Here we will import several functions from Drizzle. So import from Drizzle OM cosine distance calculates how similar or dissimilar two vectors are. De sorts in descending order. GT means greater than and SQL lets us write raw SQL expressions when needed. We will also import our database connection.
(55:08) So import DB from DB config. our schema. So, documents from DB schema and the single embedding function we created earlier. So, import generate embedding singular from dot / embeddings. Next, let's create the search function. Export async function search documents and the function takes three parameters. Query of type string which is what the user is searching for.
(55:44) Limit of type number with a default value of five which controls how many results to return. And threshold of type number with a default value of 0.5 which dictates the minimum similarity score. We will ignore results below 0.5 similarity by default. This should be number. Now within the function body first we need to convert the search query into an embedding.
(56:13) So const embedding is equal to a weight generate embedding and we pass in the query. Next we need to calculate similarity between this query embedding and all the document embeddings in our database. Results vector support helps us with this. So const similarity is equal to we calculate the cosine distance between the query embedding and all the document embeddings in our database.
(56:43) So cosine distance documents do embedding as the first argument and query embedding as the second argument. But cosine distance measures the dissimilarity between two vectors. So we will subtract from one to get the similarity. We will use SQL number template literal 1 minus dollar curly braces cosign distance. Now let's build the database query to get the most relevant documents.
(57:16) So const similar documents is equal to await db dot select. We will select the document ID that is going to be documents do ID. The content is going to be documents.content. And we will also select the calculated similarity score. And we do this from our documents table. We will then add the threshold filter.
(57:47) So dot where we call greater than passing in similarity and threshold. We only want documents with similarity above 0.5 which is the default threshold. Finally, we will sort by similarity and limit the results. So, dot order by descending of similarity and we limit to limit which is five by default. We will return similar documents from the function.
(58:18) This function is the heart of our rack system. It takes a natural language query, converts it to an embedding, searches our vector database for similar chunks, and returns the most relevant ones. The index we created earlier makes your search fast even with thousands of documents. And because we're using cosign similarity, it finds documents based on meaning, not just matching keywords. Now we have all the pieces.
(58:46) We can upload documents, search them, and chat with AI. Let's connect everything together so our chatbot can actually use the knowledge base when answering questions. Currently, our route handler just streams responses without any access to our documents. Let's give the AI a tool it can use to search the knowledge base in the rot handler present in the chat folder.
(59:14) First add tool to the list of imports from the AI package. import tool. This lets us define tools the AI can use. We will also need ZOD for the tools input schema. So, import Z from zod. We will also import the search function we just created. So, import search documents from lib/arch. Now, let's define a tool for searching the knowledge base.
(59:46) before the post handler const tools is equal to an object. The tool name is search knowledge base and we invoke the tool function we just imported. We pass in an object with description search the knowledge base for relevant information. We'll define the input schema which is z.object. It has a query property which is Z dot string and we describe it as the search query to find relevant documents and then execute is an async function.
(1:00:29) This gets access to query. So the AI will use the description to know when to use this tool and the input schema tells it what parameters to provide. Let's implement the execute function. We'll add a try catch block where we console dot error search error followed by the error and we return a string error searching the knowledge base and within the try block const is equal to await search documents passing in the user's query we limit to three relevant documents and 0.
(1:01:11) 5 as the threshold which is also the default. D so you can leave it out. Limiting to three results will keep our context focused. Now let's handle the results. If results dotlength is equal to zero, we'll return no relevant information found in the knowledge base. Otherwise, we're going to format our results. So const formatted results is equal to results dom.
(1:01:43) We pass in a callback function for each result with index. We return within square brackets index + one followed by the content and then we call dot join with two new lines. We're basically formatting the results nicely with numbered entries separated by blank lines. We will return the formatted results.
(1:02:12) Next, we need to update our message types to include the tools. So from the AI package, import infer UI tools and UI data types create the new types after the tools definition. So export type chat tools is equal to infer UI tools type of tools and export type chat message is equal to UI message never since no metadata UI data types and chat tools.
(1:02:50) These types ensure TypeScript knows about our tools throughout the application. So update the post function to use the new message type. So messages of type chat message. Next pass the tools to stream text. So tools key and value being the same. So we can use the shorthand notation and we will add a system prompt to guide the AI on when to use it.
(1:03:18) I'm going to copy paste our system prompt here. So, you're a helpful assistant with access to a knowledge base. When users ask questions, search the knowledge base for relevant information. Always search before answering if the question might relate to uploaded documents. Base your answers on the search results when available.
(1:03:39) Give concise answers that correctly answer what the user is asking for. Do not flood them with all the information from the search results. That is our system prompt. It tells AI to search the knowledge base when appropriate and base its answers on the results. And one more thing, when the AI uses a tool, that's one step. Then it needs another step to process the results and respond.
(1:04:03) So we need to allow at least two steps from the AI package. Import step count is and add it to stream text. Stop when step count is two. All right, our updated route handler is complete. The AI can now search the knowledge base when answering questions. Let's test the full rack system. First, make sure you've uploaded a PDF through the upload page.
(1:04:32) Then go to the chat interface and ask a question about the content you uploaded. For example, ask what is our vacation policy? Press enter. The AI will search the knowledge base, find relevant chunks about vacation policy and give you an accurate answer based on your actual documents. A vacation policy provides PTO as follows.
(1:05:00) For employees with 0 to 2 years, 15, 3 to 5, 20 and the vacation request should be submitted at least 2 weeks in advance through the HR portal. We can find that information right here in the PDF. Let's ask about professional development. Are there any professional development benefits? You can see yes, there are professional development benefits.
(1:05:26) The company invests in employees growth by providing an annual education budget of $2,000 per employee. A rag chatbot is working. The magic here is that the AI doesn't need to know about your documents beforehand. It searches for relevant information on demand and uses it to answer questions accurately. All right, let me now show you how to quickly implement role-based access control.
(1:05:55) So only admins such as HRs in this case can upload knowledge documents. Right now any authenticated user can upload PDFs to your knowledge base. Probably not what you want in a real application. We will use clerk's metadata feature to assign roles to users and protect this upload route. First, we need to create typescript types for our roles.
(1:06:17) So, in the applications root folder, create a new folder called types and inside create a file called globals.d.ts. Start by exporting an empty object. This makes the file a module. Now let's define what roles are available in our application. So export roles is equal to admin or user. Admins can upload documents and users can only access chat.
(1:06:50) Now we need to extend clerk's session token type. So TypeScript knows about our roles. So declare global interface custom JWT session claims metadata role is of type roles. This extends clerk's session claims globally. Now when we access the user's role anywhere in our application, TypeScript will provide autocomp completion and type safety.
(1:07:19) Next, let's update the middleware to protect the upload route. So open middleware.t ts and at the top import next response from next slashs server. We will use it to redirect non-admins. Now create a matcher for admin only routes. So const is admin route is equal to create route matcher and we just have the one upload route.
(1:07:52) So array with one entry upload. Next we update the middleware function from O. So await O. We get the session claims. So const session claims and check if the user's role is admin. const is admin is equal to session claims and if it exists metadata and if that exists check if the role is equal to admin if someone tries to access an admin route without being an admin.
(1:08:30) So if is admin route request and the user is not an admin we redirect them to the homepage. The const url is equal to new URL the root route and request dot URL return next response dot redirect to this URL. Otherwise we continue with the normal authentication check. That's the code side done. But we still need to configure clerk to include roles in the session token.
(1:09:02) Go to your clerk dashboard and under the configure tab, find the sessions page. Scroll down and find customize session token. We're going to edit this JSON. Add metadata and set it to double curly braces user.public metadata. This tells clerk to include the user's public metadata in the session token.
(1:09:30) Click save to apply the changes. Now let's set our user role as an admin. Navigate to the users tab and select your user account. It should be the only one in the list. Scroll down to the user metadata section and next to public, click edit. We will set ro to admin. Click save. And our user is now an admin. Let's test it out.
(1:09:58) Back in the browser, navigate to look host 3000/upload. We should be able to access PDF upload page. In the clerk dashboard, switch it back to user. Save it. Go back to the browser. Refresh the page and navigate to /upload. You can see we are automatically redirected to the homepage. Edit, admin again. Navigate to slashupload. and we have access. And that's it.
(1:10:29) We've built a complete rag chatbot from scratch. The user enters a prompt in the chat UI which gets sent to the route handler. The route handler sends the prompt to a search tool which does a semantic search of our vector database. It finds the relevant content, formats it, combines it with the user prompt. The AI forms a natural language response and we stream that response back to the user.
(1:10:59) We also have authentication and role-based access control. From here, you could extend this in many ways. Support more file types, add document management features, or fine-tune the chunking strategy for better results. It is completely up to you. Thank you for watching, and if you found this video helpful, please like and subscribe for more videos on how to build AI applications.


____________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
(11) The Agent Gets a Computer: Building with AI SDK v6 — Nico Albanese, Vercel - YouTube
https://www.youtube.com/watch?v=wflNENRSUb4

Transcript:
(00:15) I now have my uh live deployment here and I can continue to dashboard and I've got my project one out of five things for the production checklist. Is that all? Were you able to get that? >> Cool. >> So, we're going to jump over to the repository on GitHub that clicking that big repo button.
(00:40) And then I'm going I'm going to quit out of Slack and all these things. And I'm going to jump into a terminal. Head over to a directory that I'm happy with that I'm happy to pop this in. And I'm going to get clone that. Oops, that's not it. I need to copy it, obviously. Click the big green code button. Copy the clone and get clone AIE London demo.
(01:03) Awesome. I'm going to jump into that uh that repo there. And then if we jump back to the doc site, this is at by the way this site is if you go nicapp.comie and then that will redirect you here. Um, if you don't have the Verscell CLI installed, you can install it with so you can install that with mpmi-g Verscell.
(01:36) Uh, that will probably take you to a Verscell login first time you use it. Uh, which will pop up this um this sign up process that you can allow and then you should be signed in. And you can check that with versel. Who am I? Yes. Good thing is that in a small room like this, the Wi-Fi shouldn't be an issue. >> Were you able to log in? >> Cool.
(02:16) >> Yep. >> Perfect. the our IM team is doing awesome work with the Versel CLI. Um I'm a big fan. I am biased, but I am a big fan. Uh so once we are all set up, logged in, we go into our repository that we just cloned and we run Verscell link. Uh we say yes, should be able to find it if you have the same name.
(02:46) Uh, and then we want to pull down our environment variables. And that should give us the, which I probably shouldn't show, um, but it gets invalidated. That should give us an OIDC token which we'll be using to authenticate with both the AI gateway for our inference and for self sandbox which is yeah we we get that a lot as well where internally you can imagine we have a ton of teams I think I'm on like seven or eight different like versel teams and so authenticating with the right one can be um or finding the projects can sometimes
(03:26) be challenged, but making it easier every single time. >> Yeah, you may have already had a token in there from >> a work one. Nice. Oh, of the vers CLI. >> Oh, yeah. Yeah, that is if you have if it's installed with uh with npm, you can do npm install-g uh versel at latest and that should get
(04:35) you on to the latest one. I had that recently as well because we just invalidated the old authentication method in favor of a new one. You can also look which versel and that should tell you what what you installed it with. See mine is with pmpm instead of npm which that classic frustrating problem of which package manager owns this which for Were you able to set things up? Yeah.
(05:39) I love your shirt, by the way. >> Homemade. >> It looks It looks very professional. >> For those watching, it's Typescript shirt. It's very cool. Typescript till I die. Ding that hill. Is it getting set up or it's installing new? >> Great. >> Yeah. >> Which is the versel link. By the way, if you haven't seen, we did
(06:43) ship a new landing page. Finally, after two years, we replaced my terrible landing page. Um, which this was definitely not done by me. This is design engineers on our team. Very, very cool. Um, live download figures. I love showing this work because it's looks so good. Oh, and if you didn't know, if you've used Open Code, Open Code is all built on AI SDK as well.
(07:16) And this is the most DAX quote ever. Open code uses AI SDK. Uh, all right. We all set up. >> Cool. >> So, we're good to go. So, first thing we're going to run is uh pmppm install or npm install. Uh if you're using npm in 2026, maybe maybe use pmpp or bun. I love bun. Uh and then we can just check that it is working.
(07:48) This is a fresh xjs app, so we shouldn't have any issues. But if you run the dev server and then head to localhost 3000, you should see the wonderful um pretty new actually starting screen. >> Is that all set up for everyone? >> Yes. >> Amazing. >> Uh so we're going to start with our agent. We are going to build an agent. First thing we're going to have to do is install a few dependencies because we are working with JavaScript, right? We love installing dependencies.
(08:20) So, we're going to jump into the terminal and we're going to run pmppm add AI at AISDK react and uh ZOD. Don't worry, we're right at the beginning so you're not missing anything. Um, so we are, if you want to follow along with your computer, um, I should write it. Is there like a whiteboard or something? No, there isn't.
(08:43) Um the this site that we're on is niko albbanaz.comie. Um and that will get you to the project setup which you can follow right here. And then we're just on we're literally just starting off with building out the agent. Um so uh first thing we're doing I was saying we are building with JavaScript. So we got to install some dependencies.
(09:04) Uh we are going to be installing the AI SDK which is this very nice uh twoline npm package right here. Um our react adapter which we'll see in a little bit and zod which we'll be using for defining a few um a few schemas that we'll be using later on in the project. So last time I did an AIE workshop was in AISDK 4 and back then everything was built along pretty much four primitives which were generate text stream text generate object stream object those still exist although we've kind of pushed as much into the the text
(09:45) generation functions as possible. So you can now do structured outputs with just those generate text and stream text. Um, but we've also been working on providing a more objectoriented approach to building agents with the SDK. And part of that was like we saw in our own applications how much they were ballooning by having all of the LLM logic in the call site.
(10:08) So you had your API chat.ts and X.js route handler and in there you had 2,000 lines of code because you had the tools defined in line and you had the system prompt defined in line. And so um part of the the beauty of the AISDK is it is lightweight JavaScript. And so you can define this once in code in a monor repo and then use it anywhere whether it's a next.
(10:36) js app, it's a it's a simple bun server, whatever it is, it's plain JavaScript. Um and so this was our like first foray I would say into that which is what we call a toloop agent. Now um we have the mastermind behind the AI SDK is German. Uh and so naming is something we uh we hold quite dearly. Uh this is one of our shorter APIs but we find that this is quite obvious as to what it does. It is a tool loop using agent.
(11:04) Um kind of does what it says on the tin. Uh so to build our first agent, all we need to do is define this import this tool loop agent um and then specify a model. So we're going to do this. We're going to create a new file called um agent.ts in a lib folder. And I'm actually going to open this up in an editor so it's a little easier for me.
(11:27) And I'm going to bump up the font so everyone can see. Let's do Is that better? Can we read? Yeah. Cool. Um, so we're going to head into the app. Actually, we can do it in the top level and we're going to create a new file and it's going to be in a lib folder and it's going to be called agent.ts. Now in here we'll copy this snippet and like I said before we're importing to loop agent uh specifying our model.
(12:05) Uh the cool thing here uh if you've used the AISDK before you may have seen this syntax where you uh import a provider. So you'd import open aai from AIS if I can type at AISDKAI and then you could specify this would create a model provider instance that you could then specify a um model ID in here.
(12:32) One of the cool things that we ship with uh in AI SDK 6 is the concept of a global provider which allows you to effectively attach a provider to every single um AISDK function that you have in your application and by default that is the AI gateway and so by specifying just plain strings you can access any model in the AI gateway.
(12:56) If you want you can override this and use any provider as that global provider here. Um, but this just makes it really easy to get started. So, uh, we are going to be using, what did we say? GPT54 mini. Always hope to keep these kind of things up to date. I did this last night, so this is pretty up to date. Uh, but probably when this video goes out, this will not be.
(13:18) Um, this model will give us I haven't used 54 mini a lot. I've used 54 a ton. Um, but this gives us a nice way of kind of cheap and fast way to experiment with our agent as we go. So this is our agent definition. Kind of surprising how how little it is, but it literally is just this reusable agent using GPT5 for mini.
(13:40) Um we're then going to have to create a way to call our model and we're going to do that by building a route handler. So that's going to be in a folder called API which has a folder called chat. And then in there with NexJS's conventions, we can define a post handler in a route.ts ts file. Uh so again, that's a app API chat route.ts.
(14:07) And so one of the cool things uh going back to why you might want to use this instead of just stream text out of the box is that this allowed us to keep all of our agent definition in one place while having just the concerns for streaming from that agent in another place. And we've abstracted a lot of the complexities for that streaming into really that one line of code, this create agent UI stream response.
(14:33) Again, if you've used um the AI SDK before, what's happening here is literally the same as uh con result equals stream text. You have your stream text call and then you return result.2 UI message stream um response. Uh so it's effectively the same thing. Um, and on your agent, you also have my agent.stream, which is your agent's streaming function.
(15:04) Um, so that gives us our way of, and I'm going to get rid of this. Uh, that gives us our way of actually calling our agent. You can see that our post handler is taking in some messages and just passing that in alongside our agent. And now we need to actually create a a page um and a client to call that uh that agent or call that endpoint.
(15:29) So we can head to our page.tsx our homepage and replace it with the text that's here. And what we've got here is uh the infamous now use chat hook which was actually the first component of the SDK when we launched whatever three three years ago. Um, and this is managing all of our message state and then helping us send those messages off to our route handler.
(15:57) So we have we dstructure messages which will be our message store. It will all be on the client today. Um, error and the send message function. uh we check if there's an error and then otherwise in our markup in the UI we will map through the messages in our message state and then for each message we will map through the parts uh and we can have you used the AI SDK before is this first okay so and and you have as well so um or you haven't this is the first time seeing okay great so most of you guys have seen this so I can fly through this um but yeah this is
(16:30) how we're rendering on the client so we can now run pmpp rundev and we should now think I we should now be able to say hi and get back our response. So we've got our our chatbot or our agent as we should say it's 2026 uh ready ready to go. Now the first and most basic thing that we'll be wanting to update our agent is to change its behavior.
(17:02) One like very core way of doing that is altering its system prompt or its instructions and you can do that with the AI SDK in your agent definition by passing in an instructions uh parameter. So you can do that here. I can say respond like a cowboy. Um and now when I go back to our agent and I say hi, we should see howdy partner.
(17:27) How can I help you today? with a nice cowboy emoji as well. Um, so this goes back to I mentioned to Casper right at the beginning. Uh, what I wanted to do today is try and communicate uh what I believe to be the core building blocks for building agents in 2026 which are an agent runtime uh being a way to build your your harness effectively.
(17:53) So how do you manage the loop? How do you manage context between the loop? That kind of thing. uh the tools itself that you're passing into that runtime. And then finally, a computer uh or some kind of sandbox file system for the agent to be able to persist state or execute code uh within within its run. Uh we'll call the runs being the work the sessions that it's working on.
(18:19) And so uh this is like one of building blocks, the instructions. I think a lot of folks kind of scoffed their head at system prompt being something of 2023 when we were first really starting, but we'll see today how we're going to use the instructions alongside those two other comp really like get the most out of an agent and really uh influence its behavior.
(18:40) Um so that's the starting point. That's we've built our basic agent here. uh we're going to do the first thing that I think a lot of folks will want to do is give the agent the ability to pull in more context in some kind of way. Uh basic thing that we can do today because we don't have a great use case is just bring in web search.
(19:01) Uh so the way that we can do that is we're going to jump back to the terminal. Uh I'm going to stop my dev server here and I'm going to run this pmppm add AI SDK OpenAI. And the reason we're going to add the OpenAI provider uh is to be able to get the tool definition for something that's called a provider tool. Now there are roughly three types of tools.
(19:23) There are custom tools and I can actually find this uh right here because this is a good help for me. Um so there are three types of tools. You've got custom tools which are tools that you define yourself. Uh you provide a description, you provide an input schema and you provide an execute function. This is effectively giving the agent the ability to run any kind of arbitrary function that you want to give it based on the context of the conversation.
(19:51) Uh we then have provider defined tools. These are quite interesting. These are tools that an LLM provider will usually postrain their models to use effectively. So, Anthropic has like a bash tool which alongside all that cloud code usage that is subsidized uh they're training to use a lot better.
(20:12) Um they also have uh a tool for computer use as well. And so the idea here that you define what the agent should do when it calls it, but they've worked really hard on the description and the input schema to make sure that the agent is very effective at calling it. The final type is uh provider executed and these are tools that the that exist in the LLM provider's infrastructure.
(20:39) And so uh the classic example of this is web search web search tool. Anthropic has a web search tool as well. You don't actually provide the tool or the what should happen when the tool is called but you kind of opt yourself into the LM provider being able to use it. And if the agent decides to use it, they will literally execute that on their server and add the tool result to the message state and return all of that back to you.
(21:07) So the really nice thing about it is that we don't have to write any more code like you get it out of the box. The bad thing about these provider executed tools is you're obviously tied to a a single provider. But for today, for this demo, uh not so much of an issue and allows us to move quite quickly. Uh, and so what's happening when we when we import the OpenAI provider here is that we're literally just using this to augment our request that's going off to OpenAI ultimately to include that opt-in flag like I want the web search tool included. Um,
(21:42) so we will add that into our definition, our agent definition. And you'll see we remove our instructions as well. And actually that is literally it for us to well I need to run the dev server as well ppm rundev but I can now jump back to my agent here and say like when is AIE London AI engineer summit London I think that's what this is called and we'll see a long pause terrible UI but eventually we should see our response come back in here our augmented contacts are rag bot, if you will, using the old archaic language. Um, but our agent now
(22:24) has the ability to fetch in and pull in relevant context uh via web search. And there's some cool things that we can do here. Uh, if you pass in an object into this um into this tool uh what is this like factory function here, you can actually specify things like the user location here. And so you can say type I just use the uh language server to kind of help me here.
(22:53) But I can say that we're in London. Uh and that allows you to maybe get like uh when is AI engineer summit? So we don't include London and in theory we'll get more London oriented. Yeah, there you go. So like that extra context or those parameters are now being passed into OpenAI in into the request that's going off to open AAI ultimately.
(23:19) Uh so cool we've got a way now of augmenting our context and we've provided our agent with its first tool. Um but the problem that we have if we show this again uh when is AI engineer summit you guys already saw this. Uh I think what did I do? When is AI is that there is nothing showing up in the UI right now.
(23:49) Uh so user has no idea what's going on and uh well the agent is actually going through multiple steps there and calling a tool. So what we really want to do is actually render in our markup in this uh page.tsx. We want to render for different uh we want to render what should we want to describe what should be rendered in the UI when different tools are being used specifically when our tool web search.
(24:20) Uh so we could go in here and we could say case and then the AI SDK follows this convention of uh you a prefix of tool and then you would specify your tool name. So we'd go and we'd go to our agent and we see it's called web search. So we'd say tool web search and then we define some kind of markup here like uh let's see say like div web search.
(24:51) My god I'm typing I haven't typed in months. Uh and you can see now that our agent has called web search. Um but this process is terrible. Like what if we wanted to see like what is the p like input? There's probably a query here that's going in but it's typed as unknown. That's ugly. We don't want to do that.
(25:15) Um so this is where I'm going to delete all of that. That's why typed it and it's not on the page. This is where we are going to leverage another really awesome component of the AI SDK which is the endtoend type system that we've built. Um, so the big assumption goes through every single AI SDK API decision is like we want to have the agent definition being the source of truth that everything kind of inherits from and deres from.
(25:42) So we spend a lot of time across tool calls across the UI library making sure that it can flow nicely. Uh, and so where we start where we have to start with that is the atomic unit of state in our application which is the message. Um, and so we can get a typed message by using this type helper infer agent UI message.
(26:03) Uh, so we're going to jump back into our agent definition and pass in the type of our agent. Now this agent UI message is fully type- safe based on the tools that we pass. Um, and so we can now if we head to the route handler, we can update the route handler to show that the to define that the messages coming in are of our custom UI message type.
(26:30) But most importantly now we can go back into the page and we can type use chat as having my agent UI message. And so what you'll notice uh once you've done that and that's imported from our lib agent file. So when we scroll back down and we check for the different part types, we'll see that we should have our web search tool here now.
(26:56) And if you were to go in here and check for the um the input and output types, you'll see that they are typed, which is very cool. So, I'm not going to bore you with trying to type out and build a component on the spot because I can't design at all. Um, and I don't think any of us design anything really by hand anymore.
(27:18) But you'll see if we jump back, we now have a nice component in line for our web search. And we can try that out again and say who is Nico Alanz. Sorry, kind of egoistic. But, um, now you can see our component has that pending state, which is really cool in line. Uh so what do we have next? That's augmented our context.
(27:43) Now on to like what I think is the most interesting component also in 2026 which is providing our agent with a computer with a sandbox to interact with. Um, and to kind of take a second here, we saw this really take off where internally we have an agent called DZero, which has access to all of our like the chat with your data that we're talking about has access to pretty much all of the versel back backend, our entire admin panel, all of Salesforce, all of these kind of things.
(28:17) Uh, and the idea here was I think it was our head of data who wanted was was destroyed from getting tagged in everything and he wanted naturally a replacement Slackbot that could do all of that um, and help scale his team as well. And there was a really interesting point where it went from let's have all of this this agent that has all of these tools and it would use maybe five or 10 of them at a time and return an answer that was kind of somewhat hallucinated to when he added a file system to it and some instructions there to say okay
(28:52) every single piece of work every session that you do you have like a scratch pad in the file system which is where You're storing like an initial plan that becomes your reference for exactly what you should be doing in each step and then you have a directory that's for your research and you collect everything in there.
(29:10) And there were two things that were happening there. For one, like the agent started f like actually following through and going through entire tasks because things weren't just being layered in this insanely long context window where the initial thing was getting was getting thrown away. like the the instructions there were create this plan file and in that plan file was the objective right at the top and then right below the instructions were follow this plan file to a te check things off as you go and all of a sudden now you have this fascinating thing where the
(29:42) agent is reading and pulling in and reminding itself step okay this is my objective and so it's staying on track more and at the end you get this really great artifact that shows exactly what the agent did what work went into it. Uh and so it was kind of this emergent behavior that we saw where they were actually very good pairing this file system whether it was a full computer or just a virtual file system worked very very well.
(30:11) Um so we've now seen it across pretty much every agent that we build internally. We've got an agent for the GTM team. Uh we've got an agent like I said the data one. Um, we have a customer support one that pushed our customer support tickets down by like 90% with I think a 95% like they're people that were actually saying thank you, thank you, which we we haven't really seen before.
(30:33) All of these are backed and underpinned by this kind of file systembacked agent approach. And so that's what I wanted to show off today. Um, we are going to be using a Verscell product for this Versel sandbox and I have built a lot with different sandboxes over the years. And there's something really cool that is just shipping now that's in beta which is called um named sandboxes, persistent sandboxes, and I think I have them up right here.
(31:02) Uh, and that's what we're going to be taking advantage of today. So one of the uh issues that you have with sandboxes in general is that unless you're using it on your computer, they are fundamentally ephemeral, right? Like whether a provider has a a sandbox that can live for 30 days or five hours, at the end it's done.
(31:27) Um and so we were thinking about how we could work around those challenges. And what the team ended up coming up from is this really cool concept of like every sandbox has a name and then every sandbox can have sessions and sessions are just instances of that underlying like sandbox item we can call it.
(31:50) Um, and every time you in your product or in your code want to use a sandbox, you reference it by its name and under the scenes, Verscell will either see if there is an active instance for that session and route you to that or it will spin up a new one optimistically and route you to that. So in your code, you simplify all of this life cycle management of is my computer running, which one should I point to? I've written we'll show if I have time at the end something I've been working on that I built out an entire like life cycle management system where I was
(32:21) literally like uh taring the entire file system after every single request and then storing in blob and then like spinning up new it was terrible and this is just so much easier. So where we'll be getting to today is almost this kind of like I hate to drop the open claw thing but like we will have be able to have this specific computer that we'll be able to reference throughout every single request.
(32:48) Um and behind the scenes what will be happening is that Verscell will actually spin it down after a timeout after inactivity but it will snapshot the file system. So the state remains the same and when you make subsequent requests it will spin up a new one with that snapshotted state. So it's effectively feels like the exact same machine which is really cool.
(33:08) Um so this is in beta right now. Uh but I'm already using it a ton and it's working really really well and we've got a lot more that's coming here. So the first thing that we're going to do is we're going to jump. By the way, any questions so far? >> Yeah, of course. >> Yeah. >> Yeah. Yeah, great.
(33:47) Um, so the question because I don't know if the mic picks it up is like do you you're saying for every agent request do you send the entire history? >> I think that's the default >> with use chat. Yes. So that is the default um is right here. When we make every single request with send message, this is going to send the entire this is going to take the entire message history in the route and send that over to your agent.
(34:18) Um, now you'll always want to send or that's a bold claim. Uh, you will your your message history is your context, right? Going into to into this into your agent. And so that will always want to be something bigger, right? Like as much of your conversation history as you would like, but sending over the wire all of those messages every single time is not really something that you'll do.
(34:48) And so the the more classic pattern is sending just the most recent message here. And you can do that in your client with uh prepare. I think it's in the I don't write code anymore so I can't remember what the name is. Uh it's probably on the transport which is new. And then we have uh in here where is it? Oh, we're not importing anything from AI from AI.
(35:20) Uh I'll just show you very quickly. Actually, you know what I should be doing is just show you in the docs. That's even better. Don't know why I'm trying to type this all out. Um, basically what we will do here is you can specify to just send the most recent message and then you fetch the all of the messages on the server in your endpoint and you put those together and send those to the model.
(35:46) But where I think you're where you're leaning is more around like the context thing, right? You don't want to send old irrelevant things to the model, >> right? like the context engineering side of things of like pruning stuff and and making things go away. Is that where you're going or >> Yeah.
(36:05) Um so that's where things get harder to do uh more from a they're they're simple to do with the SDK, but it it requires tradeoffs um and it requires decisions from the developer that we don't necessarily impose from the framework. So we have a few approaches that you can do here. They're all based on using or most of them are based on using uh two things.
(36:34) For one, in um behind the scenes, what's happening in our endpoint here is that we're actually taking our UI messages, these messages, and we use uh a function under the hood called convert to model messages, which strip away a lot of the UI um specific like timestamps, ids, and all that kind of stuff.
(36:58) Um but under the hood as well you can define this I believe to like ignore certain things convert data parts to different structures to alter your context that's coming in. Um so in general like you own the messages that are coming in. So if you wanted to messages mapap and like go through and say uh if m do if message roll is I don't know uh assistant and uh m.
(37:31) artsinccludes what is it like if there's a tool call this with the typing starts getting fun. What is the type here? Basically, you can go through and and and define it like I want to strip out this type of tool call. You can totally do that. Um the the way that we see bigger code bases doing uh is actually using a function that we have called prepare step.
(37:59) And this runs before if we think about everything that was happening in our previous web search. is like agent, we send a message that spins up a new assistant message. In the first step of that assistant message, it called web search and then it sent back it basically the agent runtime took all of that context and then decided what to do next.
(38:20) And it could have used web search again, could have used the computer, whatever it might be, but each of those are like individual steps. And with the AI SDK, you can listen in. you have this callback for that runs before every single step and then this allows you to modify any of the parameters going into that specific step.
(38:42) So what do you get in here? You get like the messages, the context, the model that you're using, the step number and any of the like the steps data, what has happened. Um and then in here you can literally return any of those top level parameters. So you could return a completely different message state for like if uh let's say if step number step step number or is greater than 20 you could basically take in just where is it? These are the messages that are actively and running.
(39:16) You could say messages slice and take just the last five. And so in this way you now have running at the beginning of every single step you've got this sliding filter that is only taking the most recent five messages. Now this is a very interesting like long rabbit hole that we can go down because I've been thinking about this a lot.
(39:38) Uh, I was telling Casper before like I I've been working on a coding agent for a few months now that I have it like yesterday it ran for uh I wonder if I can find the exact photo but like it ran for 104 minutes in in one single turn. Uh let's see if we have the exact so I can prove to you these are all my I think it was right here. Yeah.
(40:04) 104 minutes it ran for used 316 tool calls changed 29 files and it used only 32% of GPT54's um context window. I have zero compaction running on this. And I think this is like kind of a hot take today because everyone's got like their own systems where they're I I previously had I was stripping away tool calls that were earlier on in the message history when I hit a certain threshold so that I would stay between 40% and 60% of the token window or the context window.
(40:41) Um, but you have a really big problem that comes up when you do that, which is you invalidate the the input cache every single time you do that. Um, which I think is like it's is it's like a oh  Yeah. Like of course. Um, but it was important when the token windows I think were like 400k. So when I when I was first building this main models I was using was Opus with I think the 400k token window and GPT53 codeex both 400k token windows and so you would hit that 40% pretty quickly but with these million token windows
(41:20) I'm not seeing as much of an issue. So what I'm building instead for it is more of the are two things. one uh more comprehensive like I have sub agents implemented here and we can talk about that a lot as well like taking dedicated pieces of work that can be somewhat independent and putting them off of the main context thread and then like returning just a thousand tokens which is the summary of whatever the objective was and bringing that back in.
(41:49) So that's just like efficient use of the context window in a session. Um but also having like a dedicated tool. Uh the AMP the folks at AMP did a really cool implementation of this is giving the agent a handoff tool which at a certain point agent can call generate some kind of context that it wants to pass into a fully new thread and that is that becomes like the main thread to start off with.
(42:19) And so I I feel as as much as you can push into sub agent territory is the because these uh summarization this compaction by agent summariz or by LLM summarization is lossy. Like you saw the there was the the infamous Twitter thread of I think the head of one of the heads of AI at at Meta AI who let her who asked her open claw like can you archive like emails from yesterday and it just deleted her entire inbox and she was like asking telling it to stop and it was like no you asked me to do this I'm going to keep going and the reason why was
(42:59) because there were so many emails that were brought in by an inefficient tool. Put that in. That triggered autocompaction and the emails overwhelmed her initial instructions. And so her initial instruction that was don't do this was gone. And so like that's what scares me a lot about like I don't have a great system.
(43:23) Whether you just keep the user messages and strip out everything else. I don't know. But that's my long tangent here. I can go into that a little bit more. My whole point here trying to say is like I don't think having having used this my coding agent for 14 hours a day for every single piece of work that I've done in the last four months compaction has not been an issue to me.
(43:45) Um and I like I I was showing you earlier I have a 95% uh cache token read ratio on on that as well. And that to me is like more valuable both in terms of like speed, performance and and cost. But that long- winded very long- winded uh rabbit hole for that and we can go into that more as well.
(44:08) But this is this is like this is how you would manipulate context between steps. And the last thing I'll mention here is like a lot of we were very um intentional with this API being functional, right? like this is a function that runs on every single step rather than like returning a uh a new message array here and persisting that to the steps afterwards.
(44:33) Like this is very easy to reason about because this is a blank like an empty function that starts at every single invocation. Your messages coming in here are the aggregated set of messages throughout. And it also gives you like when you do persist at the very end, you know that you're getting like the full message history at the end. Sorry.
(44:54) Was that helpful? >> Cool. All right. Um, and this is good as well because I know I'm kind of doing kind of basic stuff and you guys have already used it. So, please do stop me with interesting questions like that. I'm happy to to blabber. Um, so we're going to do we're getting to the fun part now.
(45:15) We have I think we installed the beta, right? Uh no, we didn't. So, we're going to go through and we're going to run this command pmppm add versel sandbox at beta. And then um we are going to create a new lib file called sandbox.ts. And like I was explaining before with these um persistent sandboxes, we're just going to create a function leverages this functionality.
(45:44) So we pass in a name and then it's going to check if that sandbox exists. If it does, it will return that. Otherwise, it will create it and return that sandbox instance. Uh so we've got that set up. Now this is some new stuff that I don't think any of you would seen if you haven't used uh AISK6 before. So we've been using the tool loop agent and we've been calling it just like this pass in our agent pass in our messages but very rarely is your is the only thing dynamic in your agent your context like a lot of the time there is different there are
(46:21) different structured inputs that should be changing the way that your agent behaves a lot of the time like the classic I give here if you have a customer support agent one of the classic things that you'll want coming into that is like the customer ID and like maybe their their um what do you call the customer type whether they're enterprise or hobby for example with Verscell and then the behavior of that model might change based on that if you're running a system for hundreds of thousands of people maybe you're an
(46:52) airline I think you're you're in travel right or no >> construction >> construction so tra travel works we we'll use that for now Imagine you're British Airways and you've got a chat bot and you are serving hundreds of thousands of customers every day with your chatbot and maybe a blue member who's like the bottom of the thing, you want to give them GPT54 mini.
(47:12) But for gold member, you want to give them GPT54 Pro or something like that. These are like structured inputs that fundamentally alter and augment the behavior. And the way that we used to do that before is like functional style. you'd say con create agent and you'd have some kind of input that would change you'd have your input here that would ultimately like change the behavior and you're doing if statements and all that kind of stuff and so we saw that and thought like these are uh it's frustrated some people online which I
(47:45) didn't realize it would these are call options they change the way that they're options that you pass at call time that change the way that the model um behaves And uh so the way that you can define those is with a call option schema that you pass in to your agent definition. So I'm going to copy over all of this code right here.
(48:12) And I may strip away some of it so it's just easier to to see. So we'll maybe we'll close this off and focus just on the call option schema. So what we want for our agent that is going to be like this agent with a computer is it should take in a sandbox in each invocation right and the agent is going to interact with that sandbox.
(48:34) So we define a zod object for our call options expected um and it's going to be an instance of our sandbox class from the versel sandbox. We can then pass in that call option schema into our tool loop agent definition. And if we were to go to our route, you can see in our create agent UI stream response, there is an options key.
(49:01) And in there, we now have this type safe uh options object that we need to pass in. You can see we've got an error here. Our options expect a sandbox. >> Howdy. >> No worries. I didn't know you have. >> Okay. Yeah. Yeah. Um, so we have our call option schema. Now we like we actually need to do something with our call options.
(49:27) And what we're going to do here, we're going to, this isn't going to make a ton of sense right now, but it will as we go into it. We want to have our sandbox available within our agent runtime for any tool to use. And the way that we're going to do it is with a kind of loaded term here is something called context with the uh within the AI SDK agent runtime.
(49:51) And what the context is is this is more similar to React context than it is agent context. And so if you're familiar with React context, you have a provider and any component within that nested uh component tree can access random arbitrary values within that. And so this is the same idea is that you can pass in any kind of arbitrary data, variables, functions, whatever they might be into this object and then you can access it in your tool runtime in your tool execute functions.
(50:25) So what we're doing here is we're saying our agent now expects a sandbox to come in every single time it's used. And then the first time it's called this prepare call function will run. It runs only once the first time that our agent is called. And then it's going to take that sandbox off of the call options and inject it into our runtime context.
(50:48) This runtime state that you have across agent invocations. Sorry, not across agent implications across the entire agent run within across steps. So that's that's the big thing. Um and I know this this is this is a lot right now. We we'll see it in a second. It will make a lot more sense. Um because we are going to define our first tool.
(51:12) So we're going to define a tool uh called in a file called tools.tcs and this is going to be a bash tool. Now we feel very strongly that bash is all you need. In a lot of cases these agents are really really good at writing bash commands. And so uh we actually for this whole session are going to have just this one tool bash that is going to do everything for us.
(51:38) So how do you define a tool with a SDK? You guys have all used ASK before but there are three components. Description super important like can't overstate it enough. This is what the model uses to decide whether to use your tool and can also influence how it uses your tool. Then input schema obviously this is what that tool needs in order to run.
(52:01) And then we have the execute function and this is the code that will be executed every single time the agent uses it. And so now you'll see where the context comes in. This is an argument on the second u the second argument of the execute function that you can pull off in your tool runtime access any of that um any of that runtime state and then run commands.
(52:22) So this is that cool way of your tool is effectively like uh wholly independent from the agent it's interacting with but expects this input to come in u and then can can use it. And we have a cool update coming to AISDK7 which actually adds typing for the context and it types the main uh context on the top level agent.
(52:51) So if you were to pull in a a tool that expects some kind of context, that will then throw an error in your agent definition for saying like you need to provide this, which is really cool. Um, and directly as a result of me pestering Lars for three months as I've been using this pattern a ton. Um, but so what we have under the scenes is like we're literally just pulling off the sandbox from the context, running our bash command that the agent generated, and then returning the standard out any errors, um, and then the exit code. So
(53:22) we could jump back and head back to our agent, which I don't think is running. So we'll run PND dev and we could say like run ls run ls-la. Nope. Why aren't we? Oh, see it's not working. Uh because we haven't provided the sandbox, of course. Um if I followed instructions, I would have known that. Uh the next step, which is well, there are two steps that are quite important.
(53:52) One, we need to actually give our I was going way ahead of myself. We need to give um our tool to our agent. So, we're going to head back to our agent definition. I'm just going to replace it all. And you'll see that now on line 21, we are passing in our bash tool that we just created.
(54:11) And then most importantly, we actually need to update our route handler to get said sandbox and pass it in to the agent run. And so we're going to do that with our uh helper function that we created before. Create or get sandbox specifying a sandbox name. So this is the this is just a random ID in an application.
(54:35) You'd have these names tied probably to a user's name or to a session. Um and that becomes this persistent sandbox that you can use across invocations. Uh so we get our sandbox here you can see and then we pass it in to our call options. You see again if you didn't pass this in we would get an error because it's uh end to end type safe and who doesn't love endtoend type safety? So the final thing that we're going to want to do uh you can it will work right now but you obviously need to wait and you don't see the terminal actually like
(55:06) the the commands running in line. So we can actually define a component for our bash tool which we'll do uh here by providing a case for tool-bash. So I'm going to jump to back to the page. I'm going to copy this in and we can look at this again. So before we had we have what should be rendered if the model generates a text um or model or the user has some text.
(55:33) What should be rendered in the UI if the model uses web search? And then finally what should be rendered in the UI if the model uses bash. Uh and this obviously I didn't create because it both looks good and um and it works. So we can say run ls-la. And now we should see our terminal that's spun up.
(55:58) It's running the command and we'll see returned that we are in a versail sandbox. So this is like uh I know this is kind of a small this is a small leap for us today but a huge jump for our agent to be able to now have this. Uh but you'll notice and I go back to instructions and behavior. If I said, "What do you see?" Agent is like oblivious.
(56:27) It now knows that it's got a bash tool, but it doesn't really know when to use it, how to use it, what to use. Um, and so this is when we could literally say, we could jump into our agents instructions here and say, you are an agent with a computer. You can access with bash. If the user asks what you can see, use ls something like that.
(56:56) This is ter terrible system prompt, but you can be like, what do you see? Naturally, like it's going to start using the computer a little bit more. Um, this is not hugely helpful right now. I think a natural progression to having access to a file system is like let's store in it, right? Um, and the pretty natural next step for storing things with an agent is memory.
(57:23) I think a lot of people are thinking about what's the ideal memory. And my hot take here is that memory is a file that you store in your sandbox and you have some kind of actual deterministic code for pulling that in, injecting it into the system prompt and then potentially having some kind of structure in your file system for different memory types.
(57:46) So you'll have like a core memories.mmd which is the stuff that gets sent in every single term and in there are probably some information like for uh to search conversation history. It's stored in conversations.jsonl or something like that. But the point being the file system becomes this playground this environment for you to store in a structured way a lot of this information.
(58:10) And the beauty here is that these agents are so good at generating bash commands that they can use things like find, like ls, like gp, like glob, all of these kinds of things. Um, so we're gonna, our final thing that we're going to play around with here is adding this concept of persistent memory. So, uh, I'm going to copy this in quickly in here.
(58:34) I I explained the basic idea of what I was thinking about which is like let's have a file in our file system called memories.mmd. Uh the agent will know to put new memories inside there and then we'll actually in that prepare call which runs once before every single agent run. Let's fetch that file and just inject it into the system prompt into the instructions with some kind of context around it.
(59:02) So that's what I'm doing here. I'm fetching the memories MD. I'm getting the the string itself and then I have the instructions from before. You're a coding agent with access to a computer. You have a memories.mmd file that you can read and write to. You should always add any facts the user shares to memories.mmd. And then if we have any memories here are your memories. Otherwise, no memories yet.
(59:21) And we could even ask something like no, actually that's fine. We can go like this. And so now I could say, hey, my name is Nico. Again, really kind of dumb dumb examples here, but we'll see the the beginning of it. User's name is Nico. And now, if I refresh and I say hi, we'll see that it's doing some behavior that we don't want, which is it's adding some kind of arbitrary like user greeted with hi that.
(59:52) But you can see that it did regardless like it had Nico in the file system. And I could say something here like uh don't report every only record important memories. >> You're you're absolutely right. >> Important facts the user shares only record important memories. Let's try. You see this this explains what's broken with most agents is that like me even as a native English speaker kind of uh writes conflicting instructions that an agent will obviously take very literally.
(1:00:37) So I say hi and now hopefully it's done it again. So so you see like this is so much of this this is also like a kind of a dumber model uh with or no we moved up to GPT54. So, it's not even uh I would say don't don't share greetings. And this is bad because you obviously this is like the per the pink elephant. You don't really want to mention behavior you don't want it to do because it's now activating parameters and we don't really know what it's doing.
(1:01:06) But let's uh let's see.  man really. Uh so that doesn't Yeah, exactly. The fact that nuke my memories. Uh, so let let's get rid of delete them. Okay, great. Let's remove the So now, I mean, this is the this is kind of the fun part of building this is like we're we're literally trying to get this kind of machine to use this persistent state.
(1:01:38) And you'll see that I I find this so having built so much with sandboxes now the fact that we just have that ID that name and it's just persisting whether it's alive or whether it's actively running or not but we have that persistent state is really cool. I did work on some prompts before that do make the memory system better.
(1:01:58) So you could see do not save trivial interactions like greeting small talk or information that can be derived from the codebase itself. So we're going to copy this over and see how this does. Um, and now hopefully when we go hi, I've also made this added system prompts here to make it a little bit more inquisitive.
(1:02:17) So, a bit like open clause like onboarding was like what's your name? Do you do so like hi I'm Nico. Um, and so now it's adding users name. What do you do? I work on why am I doing this? I work on the AI SDK at Verscell. I don't know why I was typing so much today. Uh, so then we get that added and now when I pop in to a new chat, hi.
(1:02:43) Like we've got that memory that's persisted over these states. Now another really cool thing that you can start doing just by leveraging the bash that we have here is using something we already have in the environment and something the agent is already very good at which is generating code and executing code. And so a classic thing that you'll see which is I think is why open cloud was so exciting and why agents have really taken off this year is this idea of agents modifying or extending themselves and a big part of that is less about modifying
(1:03:17) itself but it's about like creating these giving it the feedback loop in the environment where it can build run code evaluate the output and uh and iterate on that and I think that's also why coding has become naturally like the first place this has really gone crazy is like we've got compilers, we've got type checkers.
(1:03:38) It's like it is the perfect environment for that uh basically ripple uh loop. Um, but so I've added another kind of thing here that telling the agent if there are any repeatable tasks like make a Python script out of it and then inject those Python scripts into or the description of it into the end of our memories MD and use that as like a core or or use that to decide what to do next.
(1:04:06) So if the user asks you to get the weather, make a weather script. And if the user asks to get the weather again, use that script that you already have access to. And this is like these are the building blocks of this agent that learns that builds what builds on itself as we go. And so we can give this a go.
(1:04:23) We can see how it will do. Um we say get the weather in London. Uh use Python. I mean, this is a I'm going to force it a little bit here given we're doing a demo. Uh, and we'll see what it does. So, it's going to write some Python here and we'll see it's it's gotten the weather and now it's added that to our our memories MD and it's pretty cool.
(1:04:54) Like, this was all one assistant turn just using bash and now it's got this tool for for getting the weather. So now we could see get weather NSF. Let's try. Will it work? And look, it used it used it is not minus 11 uh because it got Quebec Canada for some reason. We'll have to modify it uh slightly. Yes. Do this. Do this. Oops.
(1:05:27) I probably broke everything here. Um, amazing. It actually worked. Okay, so it's 12 degrees Celsius. But you can see now like we've got this um I prefer. See, it's cool. Like the more that it's asking, the more that it's building itself, the more that it's learning about what I like. And now in this computer, this is like my agent's dedicated playground and workspace for it to help me out with anything.
(1:05:52) So that was the basis for what I wanted to go through today. Okay. What I did want to show you is like what you can I've I've built a very complex agent system on top of these exact patterns that I use now every single day to get my work done. Um so I wanted to I mean shill this is coming out later today.
(1:06:16) I'm holding myself to that. And this is effectively like uh cursor background agents uh but using those concepts that we had today. uses AI SDK, uses that exact pattern, um uses um AI gateway for inference, uses workflow, so it can run infinitely and each of the steps are durable. So each LLM step is matched to a durable workflow step and if any step fails, it literally retries until it gets um it gets a result.
(1:06:50) Um, and yeah, this also builds on that uh what we were talking about before with sub agents. So like I can create any new session um and be like, "Yeah, we have to finish. I'm done anyway. This is more me showing off." But um spin up a uh sub agent to explore this project and um after it creates because this is on my hotspot so it's a little bit slow.
(1:07:20) kind of see effectively what I'm trying to show is like this this uh pattern scales to a pretty large system uh and to show you like this is being used by uh so yeah we've got sub agent spinning up um and this runs in the background like this is being used by 23 people at versell I've put three and a half 3.
(1:07:44) 8 8 billion tokens through this uh in the last like month or two. That cash read ratio I was telling you I'm very proud of. 90 91% uh responsible for close to 350 PRs although this doesn't include all of them that closed. Um, and yeah, and you'll see like this will, if we go back here, has resumable streams, and our, you can see our sub agent was going off finding, searching all of this stuff off of the main thread, use 30,000 tokens, but just returned like 500 at the very end.
(1:08:18) So that keeps us within literally 7,000 tokens for the main like agent thread. So yeah, and this hopefully will be out later today. I'm obviously here if you guys have questions after if we're getting kicked out. Um, yeah.